# 🚀 [GUIDE] Fixing DaVinci Resolve 20.2.2 Crash on Startup in Fedora 43

🎬 Linux | Fedora | DaVinci Resolve | Troubleshooting | Open-Source

بعد از آپگرید به **Fedora 43** با یک مشکل عجیب روبه‌رو شدم:  
**DaVinci Resolve 20.2.2 درست بعد از صفحه Project Manager کرش می‌کرد و بسته می‌شد.**

این مشکل فقط مخصوص Linux Users نیست؛ هرکسی که روی Fedora کار می‌کند ممکن است با این Crash روبه‌رو شود.  
بعد از چند ساعت بررسی، متوجه شدم مشکل اصلی از **ناسازگاری Python 3.13** (نسخه جدید Fedora) با کتابخانه‌های داخلی Resolve است.

خبر خوب؟  
رفع‌ش کاملاً ممکن است 🔧

---

## 🔍 ریشه مشکل

Fedora 43 به‌صورت پیش‌فرض از **Python 3.13** استفاده می‌کند، اما DaVinci Resolve هنوز وابسته به **Python 3.11** است.  
به همین دلیل هنگام Load شدن کتابخانه‌های گرافیکی و Qt، برنامه **Crash** می‌کند.

---

## ✅ راه‌حل کاربردی (و تست‌شده)

برای حل کامل این مشکل، سه کار باید انجام شود:

### 1️⃣ نصب Python 3.11

```bash
sudo dnf install python3.11 python3.11-libs
```

### 2️⃣ ساخت یک Wrapper Script

این اسکریپت باعث می‌شود Resolve به‌جای Python 3.13 از **libpython3.11.so** استفاده کند و با **X11/XCB** اجرا شود (به‌جای Wayland که باعث کرش می‌شود).

مسیر زیر را بسازید:

```bash
nano ~/.local/bin/resolve-wrapper
```

و این را داخلش قرار دهید:

```bash
#!/usr/bin/env bash
LIB="/usr/lib64/libpython3.11.so.1.0"
export LD_PRELOAD="$LIB${LD_PRELOAD:+:$LD_PRELOAD}"
export QT_QPA_PLATFORM=xcb
export __GL_THREADED_OPTIMIZATIONS=0
export QT_SCALE_FACTOR=2
export QT_DEVICE_PIXEL_RATIO=2
export LD_LIBRARY_PATH="/opt/resolve/libs:/usr/lib64${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
exec /opt/resolve/bin/resolve "$@"
```

اجراپذیرش کنید:

```bash
chmod +x ~/.local/bin/resolve-wrapper
```

### 3️⃣ تغییر Desktop Entry

در مسیر زیر:

```
~/.local/share/applications/com.blackmagicdesign.resolve.desktop
```

بخش Exec را تغییر دهید به:

```
Exec=/home/<username>/.local/bin/resolve-wrapper %U
```

---

## 🎉 نتیجه

بعد از اعمال این مراحل:

✔ DaVinci Resolve بدون هیچ Crash اجرا می‌شود  
✔ مشکل Python Compatibility حل می‌شود  
✔ مشکل Wayland و Qt رفع می‌شود  
✔ HiDPI Scaling عالی عمل می‌کند  
✔ تجربه ویرایش ویدیوی کاملاً پایدار روی Fedora 43

---

## 📌 اگر شما هم روی Linux، Fedora، یا محیط‌های Open-Source کار می‌کنید…

حتماً این پست را ذخیره کنید. این مشکل احتمالاً کاربران زیادی را در ماه‌های آینده درگیر خواهد کرد.

اگر سؤالی دارید یا نیاز به کمک بیشتر دارید، خوشحال می‌شوم در موردش صحبت کنیم 🤝

---

اگر خواستی نسخه انگلیسی هم برات بسازم یا یک نسخه کوتاه‌تر مخصوص الگوریتم لینکدین بدم—فقط بگو.